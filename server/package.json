{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:client" :"cd ../frontend && ng serve --open",
    "start:server" :"nodemon server.js",
    "dev":"concurrently -k --kill-others-on-fail \"yarn run start:client\" \"yarn run start:server\" "
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  }
}
